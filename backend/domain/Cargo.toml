[package]
name = "run-crab-run-domain"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = { path = "../spacetimedb/crates/bindings", version = "2.1.0" }
log = "0.4"